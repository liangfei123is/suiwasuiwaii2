1. **修改HTML文件结构**

   * 在`<body>`标签开头添加启动页面的HTML结构

   * 启动页面包含开场白内容：主文字和律师姓名

   * 使用现有的CSS类名：`splash-screen`, `splash-content`, `splash-opening`

2. **设置开场白内容**

   * 主文字："法律不仅是保护被欺凌者的，也是保护欺凌者有一天不被欺凌的。"（重复两次）

   * 律师姓名："朱爱丽律师"

   * 使用现有的CSS类名：`main-text`, `name-text`

3. **添加JavaScript自动跳转功能**

   * 在HTML文件末尾添加`<script>`标签

   * 编写代码：页面加载后2秒自动隐藏启动页面，显示主内容

   * 使用`setTimeout`函数实现延迟效果

   * 添加平滑过渡动画

4. **确保样式正确应用**

   * 检查现有CSS样式是否满足需求

   * 如需调整，修改CSS文件中的启动页面相关样式

5. **测试实现效果**

   * 确保启动页面正确显示

   * 确保2秒后自动跳转到主页面

   * 确保响应式设计正常工作

